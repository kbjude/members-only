<h1> Posts </h1>

<% if user_signed_in? %>
    <ul>
        <% @posts.each do |post|%>
            <li>
                <p> <h1> <%= post.title %> </h1> <p>
                <%= link_to post.body %> <br>
                <%= post.user.name %>
            </li>
        <% end %>
    </ul>
<% end %>